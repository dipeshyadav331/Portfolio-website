import React from 'react'

function education() {
    const handleClick = () => {
        window.location.hash = "#name";
    };

    return (
        <section id="education">
            <h1>Education</h1>
        </section>
    )
}

export default education